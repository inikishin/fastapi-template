[pytest]
markers =
    smoke: subset of tests that should be run before each commit
addopts =
    --strict-markers --disable-warnings --cov=src --cov-report html
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session